Key word 
Forgein key = prime key + foreign key บ.แม่ลูก บ.ลูก
    JOIN
    1.INNER JOIN = JOIN = หาเฉพาะข้อมูลที่ถูกบันทึกไว้ ,  ถ้าสนใจหลายคอลัมให้ใส่ comma or ',' like i1,i2,n =intersec
    - when 
        only matching record both toble.
        look oreder record relation in both table.
    - technique query 1.must set a ref/origin table ต้องตั้งตารางตั้งต้นไว้เสมอ **
    
    -ตัวอย่าง-
    SELECT customer.firstname ,customer.lastname,invoice.Total = select interested column data form table 
    FROM customer I have a data on customer
    INNER JOIN invoice = but I interested in data on customer will u join table from condition
    ON customer_id(primary) = invoice.customre_id(foreign)  = at condition look this left match right หาข้อมูลที่มันแมทช์กันของสองตารางที่ชี้ให้
    
    2.LEF JOIN = Show all record from one table [ไม่สนแมทช์] but righ table focus only match.
    3.RIGHT JOIN = Swith from 2. main table if not match haven't show but show all from sub table.
    4.FULL OUTTER JOIN = opposite INNER JOIN  show all record overlook match or unmatch = union
 note primary key = ข้อมูลแต่ละอย่างในคอลัมหนึ่งจะไม่ซ้ำกัน foreign key = การเชื่อมโยงข้อมูลที่เหมือนกัน

 จำลอง one to many ลูกค้าคนนึง สั่งได้หลายออเดอร์ แต่ออเดอร์รับลูกค้าได้1คน
 Normalization concept = clean data before use  because make a same data format.
 1NF = atomic weight ; slipt represent form
 2NF = removing partial dependencies , whole primary key ; separating customer vs order
 3NF = eliminateing transitive dependencies , only on the primary key ; seperate product
 
 DATA objects
    TABLES = primary data
    Indices = INDEX IMPORTANT WITH DATABASE & MONGODB
     is ที่คั่นหนังสือ ,สารบัญ ช่วยหาข้อมูลได้เร็วขึ้น
    Views = การสร้างตารางจำลองใหม่
    Trigger = embed function to automation action

CTE = create new table  //
 WITH cte_name AS (
    function
 )
 Temporary table = สร้างมาใช้ชั่วคราว สำหรับ current session
 SELECT xxx AS set ชื่อ 
ORDER BY เรียงลำดับจากน้อยไปมาก หรือ มากไปน้อย